FROM node:22-slim

WORKDIR /app
RUN npm init -y && npm install @playwright/mcp@latest
RUN npx playwright install --with-deps chromium

EXPOSE 3025

CMD ["npx", "@playwright/mcp@latest", "--host", "0.0.0.0", "--port", "3025", "--browser", "chromium", "--headless", "--allowed-hosts", "*"]
