FROM node:22-slim

# Install Playwright MCP and its browser dependencies
RUN npx -y @playwright/mcp@latest --help > /dev/null 2>&1 && \
    npx playwright install --with-deps chromium

EXPOSE 3025

ENV HOST=0.0.0.0
CMD ["npx", "@playwright/mcp@latest", "--port", "3025"]
