FROM python:3.12-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir \
    paho-mqtt \
    influxdb-client

# Copy the subscriber script
COPY scripts/ruuvi_mqtt_subscriber.py /app/

CMD ["python", "-u", "ruuvi_mqtt_subscriber.py"]
